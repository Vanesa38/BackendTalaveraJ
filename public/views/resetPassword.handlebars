<div>
    <h1>Restablecer contraseña</h1>
    <form action="/reset" method="post">
      <input type="hidden" name="token" value="{{token}}">
      <label for="password">Nueva contraseña:</label>
      <input type="password" name="Newpassword" id="password">
      <label for="repeatPassword">Confirmar contraseña:</label>
      <input type="password" name="repeatNewPassword" id="repeatPassword">
      <button id="restablecer"type="submit">Restablecer contraseña</button>
    </form>
</div>
